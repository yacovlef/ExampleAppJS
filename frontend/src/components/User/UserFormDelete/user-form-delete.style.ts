import styled from 'styled-components';

const UserFormDeleteForm = styled.form``;

const UserFormDeleteRow = styled.div`
    padding: 0.5rem 0;
    text-align: center;
`;

const UserFormDeleteTitle = styled.span`
    font-size: 1.5rem;
`;

export {
    UserFormDeleteForm,
    UserFormDeleteRow,
    UserFormDeleteTitle
};
