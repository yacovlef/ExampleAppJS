import styled from 'styled-components';

const FooterWrapper = styled.footer`
    flex: 0 0 auto;
`;

const FooterLayout = styled.div`
    text-align: center;
    padding: 1rem 0;
`;

export {
    FooterWrapper,
    FooterLayout
}
