import styled from 'styled-components';

const LoadingLayout = styled.div`
    text-align: center;
`;

const LoadingText = styled.span`
    font-size: 2rem;
`;

export {
    LoadingLayout,
    LoadingText
}
