import styled from 'styled-components';

const IndexLayout = styled.div`
    text-align: center;

    padding: 1rem;
`;

const IndexTitle = styled.span`
    font-size: 2rem;
`;

export {
    IndexLayout,
    IndexTitle
};
